(()=>{
 if(typeof openCustomer!=='function')return;
 const original=openCustomer;
 const h=v=>typeof esc==='function'?esc(v):String(v??'');
 openCustomer=async function(id){await original(id);const box=document.querySelector('.customer-detail-dialog [data-detail]'),pane=box?.querySelector('[data-pane="master"]');if(!pane||pane.querySelector('[data-customer-master]'))return;try{const data=await post('/api/v1/customers/detail',{id}),m=data.customer?.master||{};const article=document.createElement('article');article.dataset.customerMaster='';article.className='customer-master-card';article.innerHTML=`<div class="panel-head"><div><p class="eyebrow">STAMMDATEN</p><h3>Unternehmensdaten</h3></div></div><form class="customer-master-form"><div class="field-grid"><label>Namenszusatz<input name="name_addition" value="${h(m.name_addition||'')}"></label><label>Kundennummer<input name="customer_number" value="${h(m.customer_number||'')}"></label></div><div class="field-grid"><label>Straße<input name="street" value="${h(m.street||'')}"></label><label>Hausnummer<input name="house_number" value="${h(m.house_number||'')}"></label></div><div class="field-grid"><label>PLZ<input name="zip_code" value="${h(m.zip_code||'')}"></label><label>Ort<input name="city" value="${h(m.city||'')}"></label></div><div class="field-grid"><label>Land<input name="country" value="${h(m.country||'Deutschland')}"></label><label>Website<input name="website" value="${h(m.website||'')}" placeholder="https://..."></label></div><div class="field-grid"><label>Rechnungs-E-Mail<input type="email" name="billing_email" value="${h(m.billing_email||'')}"></label><label>Zahlungsbedingungen<input name="payment_terms" value="${h(m.payment_terms||'')}"></label></div><div class="field-grid"><label>Steuernummer<input name="tax_number" value="${h(m.tax_number||'')}"></label><label>USt-IdNr.<input name="vat_id" value="${h(m.vat_id||'')}"></label></div><label>Rechnungs-/interne Hinweise<textarea name="invoice_note" rows="3">${h(m.invoice_note||'')}</textarea></label><button class="primary">Stammdaten speichern</button></form>`;pane.prepend(article);article.querySelector('form').onsubmit=async ev=>{ev.preventDefault();const payload=Object.fromEntries(new FormData(ev.currentTarget));payload.customer_id=id;payload.save=true;try{await post('/api/v1/customers/master',payload);window.pzToast?.('Kundenstammdaten gespeichert','success');}catch(e){window.pzToast?.(e.message,'error');}};}catch(e){window.pzToast?.(`Stammdaten konnten nicht geladen werden: ${e.message}`,'error');}};
})();
